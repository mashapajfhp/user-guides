{
  "name": "user-guides-playwright-validation",
  "version": "1.0.0",
  "description": "Playwright validation for user guides",
  "private": true,
  "scripts": {
    "test": "playwright test",
    "validate": "node scripts/playwright/run-validation.mjs"
  },
  "dependencies": {
    "@playwright/test": "^1.40.0",
    "playwright": "^1.40.0"
  },
  "engines": {
    "node": ">=18"
  }
}
