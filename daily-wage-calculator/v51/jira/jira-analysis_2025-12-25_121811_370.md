 =# Jira Validation Pack — daily wage calculator

Generated on: 2025-12-25
Source: jira

## Summary

- Total claims: 71
- Runnable in Playwright: 49
- Blocked / non-Playwright: 22

## FEATURE IDENTITY (AUTHORITATIVE — DO NOT OVERRIDE)

- Feature name: daily wage calculator
- Clean feature name: daily_wage_calculator
- Source: jira
- Analysis date: 2025-12-25

---

## Limitation

### Month-Specific Day Count for Wage Calculations

**Statement:** Daily wage calculations must account for actual calendar days in each month, especially February in leap years

**Meta:** id=JIRA_015 | playwright=YES | test_data=YES | limitation_type=calculation_logic

**Evidence (tickets):** TSSD-1301

**Preconditions:**
- Prepare test data to reproduce the scenario described by the ticket(s)

**Steps:**
- Log in as a user who can access the feature (admin if required)
- Navigate to the feature area relevant to this claim

**Expected:**
- Result is observable (in UI or logs) and consistent with the claim statement

**Selector hints:**
- Prefer data-testid selectors (or stable automation IDs) where available
- Fallback: locate by visible text labels and section headings

### Leave Deduction Calculation Dependent on Salary Config Timing

**Statement:** Unpaid leave deductions are calculated based on salary configuration existing at leave request creation time

**Meta:** id=JIRA_017 | playwright=YES | test_data=YES | limitation_type=configuration_constraint

**Evidence (tickets):** TSSD-1581

**Preconditions:**
- Prepare test data to reproduce the scenario described by the ticket(s)

**Steps:**
- Log in as a user who can access the feature (admin if required)
- Navigate to the feature area relevant to this claim
- Open the relevant Settings / Configuration section for the feature

**Expected:**
- Result is observable (in UI or logs) and consistent with the claim statement

**Selector hints:**
- Prefer data-testid selectors (or stable automation IDs) where available
- Fallback: locate by visible text labels and section headings

### Ambiguous Day Counting Methodology

**Statement:** Unclear rules for counting departure date and handling leap year days in service duration calculation

**Meta:** id=JIRA_020 | playwright=NO | test_data=YES | limitation_type=calculation_methodology | blocker=BACKEND_ORACLE_REQUIRED

**Evidence (tickets):** TSSD-1625

**Preconditions:**
- Prepare test data to reproduce the scenario described by the ticket(s)

**Steps:**
- Log in as a user who can access the feature (admin if required)
- Navigate to the feature area relevant to this claim

**Expected:**
- Result is observable (in UI or logs) and consistent with the claim statement

**Selector hints:**
- Prefer data-testid selectors (or stable automation IDs) where available
- Fallback: locate by visible text labels and section headings

### Global Settings Override Individual Policy Configurations

**Statement:** Users cannot customize leave deduction calculations at the policy level when a global setting exists

**Meta:** id=JIRA_024 | playwright=YES | ui=YES | test_data=YES | limitation_type=configuration_hierarchy

**Evidence (tickets):** TSSD-1753

**Preconditions:**
- Prepare test data to reproduce the scenario described by the ticket(s)

**Steps:**
- Log in as a user who can access the feature (admin if required)
- Navigate to the feature area relevant to this claim
- Open the relevant Settings / Configuration section for the feature
- Locate the UI element referenced in the claim (label/field/value/tooltip/etc.)

**Expected:**
- UI state matches the claim expectation and does not show contradictory information

**Selector hints:**
- Prefer data-testid selectors (or stable automation IDs) where available
- Fallback: locate by visible text labels and section headings

### Daily Rate Calculation Dependent on Payroll Month

**Statement:** Daily rate is dynamically recalculated based on the current payroll month, not stored from original calculation

**Meta:** id=JIRA_028 | playwright=NO | test_data=YES | limitation_type=calculation_logic | blocker=ASYNC_RECALC_OR_TXN_LIFECYCLE

**Evidence (tickets):** TSSD-1807

**Preconditions:**
- Prepare test data to reproduce the scenario described by the ticket(s)

**Steps:**
- Log in as a user who can access the feature (admin if required)
- Navigate to the feature area relevant to this claim

**Expected:**
- Result is observable (in UI or logs) and consistent with the claim statement

**Selector hints:**
- Prefer data-testid selectors (or stable automation IDs) where available
- Fallback: locate by visible text labels and section headings

### Fixed Daily Wage Divisor

**Statement:** Daily wage always calculated as (Basic Salary / 30), cannot be modified

**Meta:** id=JIRA_032 | playwright=NO | limitation_type=calculation_constraint | blocker=BACKEND_ORACLE_REQUIRED

**Evidence (tickets):** TSSD-2043

**Preconditions:**
_None_

**Steps:**
- Log in as a user who can access the feature (admin if required)
- Navigate to the feature area relevant to this claim

**Expected:**
- Result is observable (in UI or logs) and consistent with the claim statement

**Selector hints:**
- Prefer data-testid selectors (or stable automation IDs) where available
- Fallback: locate by visible text labels and section headings

### Complex Leap Year Day Counting

**Statement:** System uses 366 days as divisor for leap years when calculating partial service periods, which may differ from manual expectations

**Meta:** id=JIRA_035 | playwright=NO | limitation_type=calculation_methodology | blocker=BACKEND_ORACLE_REQUIRED

**Evidence (tickets):** TSSD-2252

**Preconditions:**
_None_

**Steps:**
- Log in as a user who can access the feature (admin if required)
- Navigate to the feature area relevant to this claim

**Expected:**
- Result is observable (in UI or logs) and consistent with the claim statement

**Selector hints:**
- Prefer data-testid selectors (or stable automation IDs) where available
- Fallback: locate by visible text labels and section headings

### Separate Calculation Methods for Leave and Proration

**Statement:** Leave salary and salary proration use different divisors and cannot be automatically synchronized

**Meta:** id=JIRA_040 | playwright=NO | test_data=YES | limitation_type=calculation_configuration | blocker=BACKEND_ORACLE_REQUIRED

**Evidence (tickets):** TSSD-2321

**Preconditions:**
- Ensure integration prerequisites are configured (if applicable)
- Prepare test data to reproduce the scenario described by the ticket(s)

**Steps:**
- Log in as a user who can access the feature (admin if required)
- Navigate to the feature area relevant to this claim

**Expected:**
- Result is observable (in UI or logs) and consistent with the claim statement

**Selector hints:**
- Prefer data-testid selectors (or stable automation IDs) where available
- Fallback: locate by visible text labels and section headings

### Decimal Precision Limitation in Pro-Rated Calculations

**Statement:** System calculates daily wage (basic + allowances / 30) with consistent minor decimal point discrepancies

**Meta:** id=JIRA_042 | playwright=NO | test_data=YES | limitation_type=calculation_precision | blocker=BACKEND_ORACLE_REQUIRED

**Evidence (tickets):** TSSD-2605

**Preconditions:**
- Prepare test data to reproduce the scenario described by the ticket(s)

**Steps:**
- Log in as a user who can access the feature (admin if required)
- Navigate to the feature area relevant to this claim

**Expected:**
- Result is observable (in UI or logs) and consistent with the claim statement

**Selector hints:**
- Prefer data-testid selectors (or stable automation IDs) where available
- Fallback: locate by visible text labels and section headings

### No Automatic Formula Synchronization

**Statement:** Daily wage formula must be manually aligned between leave policy and payroll modules

**Meta:** id=JIRA_047 | playwright=NO | ui=YES | test_data=YES | limitation_type=configuration_sync | blocker=BACKEND_ORACLE_REQUIRED

**Evidence (tickets):** TSSD-2648

**Preconditions:**
- Ensure integration prerequisites are configured (if applicable)
- Prepare test data to reproduce the scenario described by the ticket(s)

**Steps:**
- Log in as a user who can access the feature (admin if required)
- Navigate to the feature area relevant to this claim
- Locate the UI element referenced in the claim (label/field/value/tooltip/etc.)

**Expected:**
- UI state matches the claim expectation and does not show contradictory information

**Selector hints:**
- Prefer data-testid selectors (or stable automation IDs) where available
- Fallback: locate by visible text labels and section headings

### Working Days Impacts Leave Salary Calculation

**Statement:** Leave encashment daily wage calculation varies based on monthly working days configuration

**Meta:** id=JIRA_050 | playwright=YES | test_data=YES | limitation_type=configuration_limitation

**Evidence (tickets):** TSSD-3648

**Preconditions:**
- Prepare test data to reproduce the scenario described by the ticket(s)

**Steps:**
- Log in as a user who can access the feature (admin if required)
- Navigate to the feature area relevant to this claim
- Open the relevant Settings / Configuration section for the feature

**Expected:**
- Result is observable (in UI or logs) and consistent with the claim statement

**Selector hints:**
- Prefer data-testid selectors (or stable automation IDs) where available
- Fallback: locate by visible text labels and section headings

### Incorrect Day Counting for Leap Years

**Statement:** System defaults to 365 days per year, failing to recognize leap years with 366 days

**Meta:** id=JIRA_052 | playwright=YES | limitation_type=calculation_logic

**Evidence (tickets):** TSSD-4179

**Preconditions:**
_None_

**Steps:**
- Log in as a user who can access the feature (admin if required)
- Navigate to the feature area relevant to this claim

**Expected:**
- Result is observable (in UI or logs) and consistent with the claim statement

**Selector hints:**
- Prefer data-testid selectors (or stable automation IDs) where available
- Fallback: locate by visible text labels and section headings

### Salary Calculation Tied to Work Week Configuration

**Statement:** Salary proration is strictly based on work week configuration, with no automatic weekend detection

**Meta:** id=JIRA_056 | playwright=YES | test_data=YES | limitation_type=configuration_constraint

**Evidence (tickets):** TSSD-4292

**Preconditions:**
- Prepare test data to reproduce the scenario described by the ticket(s)

**Steps:**
- Log in as a user who can access the feature (admin if required)
- Navigate to the feature area relevant to this claim
- Open the relevant Settings / Configuration section for the feature

**Expected:**
- Result is observable (in UI or logs) and consistent with the claim statement

**Selector hints:**
- Prefer data-testid selectors (or stable automation IDs) where available
- Fallback: locate by visible text labels and section headings

### Fixed 30.42 Days Divisor for Calculations

**Statement:** System uses a fixed 30.42 days divisor for daily wage calculations, which may not align with calendar days

**Meta:** id=JIRA_057 | playwright=NO | test_data=YES | limitation_type=calculation_method | blocker=BACKEND_ORACLE_REQUIRED

**Evidence (tickets):** TSSD-4368

**Preconditions:**
- Prepare test data to reproduce the scenario described by the ticket(s)

**Steps:**
- Log in as a user who can access the feature (admin if required)
- Navigate to the feature area relevant to this claim

**Expected:**
- Result is observable (in UI or logs) and consistent with the claim statement

**Selector hints:**
- Prefer data-testid selectors (or stable automation IDs) where available
- Fallback: locate by visible text labels and section headings

### Inflexible Leave Salary Calculation

**Statement:** Platform's leave salary calculation is rigidly tied to predefined policy configurations, which may not flexibly adapt to varying month lengths or specific employee scenarios

**Meta:** id=JIRA_062 | playwright=YES | ui=YES | test_data=YES | limitation_type=configuration_constraint

**Evidence (tickets):** TSSD-4731

**Preconditions:**
- Prepare test data to reproduce the scenario described by the ticket(s)

**Steps:**
- Log in as a user who can access the feature (admin if required)
- Navigate to the feature area relevant to this claim
- Open the relevant Settings / Configuration section for the feature
- Locate the UI element referenced in the claim (label/field/value/tooltip/etc.)

**Expected:**
- UI state matches the claim expectation and does not show contradictory information

**Selector hints:**
- Prefer data-testid selectors (or stable automation IDs) where available
- Fallback: locate by visible text labels and section headings

### Mid-Month Departure Leave Accrual Limitation

**Statement:** Leave days not prorated or included for incomplete months of departure

**Meta:** id=JIRA_067 | playwright=YES | test_data=YES | limitation_type=accrual_policy

**Evidence (tickets):** TSSD-4742

**Preconditions:**
- Prepare test data to reproduce the scenario described by the ticket(s)

**Steps:**
- Log in as a user who can access the feature (admin if required)
- Navigate to the feature area relevant to this claim

**Expected:**
- Result is observable (in UI or logs) and consistent with the claim statement

**Selector hints:**
- Prefer data-testid selectors (or stable automation IDs) where available
- Fallback: locate by visible text labels and section headings

### No Automatic Salary Adjustment for Working Days

**Statement:** Current system cannot dynamically adjust salaries based on actual working days, requiring manual tracking of overtime and unpaid leaves.

**Meta:** id=JIRA_070 | playwright=YES | ui=YES | limitation_type=feature_gap

**Evidence (tickets):** TSSD-4905

**Preconditions:**
_None_

**Steps:**
- Log in as a user who can access the feature (admin if required)
- Navigate to the feature area relevant to this claim
- Locate the UI element referenced in the claim (label/field/value/tooltip/etc.)

**Expected:**
- UI state matches the claim expectation and does not show contradictory information

**Selector hints:**
- Prefer data-testid selectors (or stable automation IDs) where available
- Fallback: locate by visible text labels and section headings

## Edge Case

### Leap Year Edge Case

**Statement:** Handling of February 29th in service duration calculation

**Meta:** id=JIRA_022 | playwright=YES | test_data=YES

**Evidence (tickets):** TSSD-1625

**Preconditions:**
- Prepare test data to reproduce the scenario described by the ticket(s)

**Steps:**
- Log in as a user who can access the feature (admin if required)
- Navigate to the feature area relevant to this claim

**Expected:**
- System behavior matches expected handling for this edge case (no unexpected errors)

**Selector hints:**
- Prefer data-testid selectors (or stable automation IDs) where available
- Fallback: locate by visible text labels and section headings

### Salary Component Calculation Mismatch

**Statement:** Deduction calculated using gross salary instead of basic salary

**Meta:** id=JIRA_025 | playwright=YES | test_data=YES

**Evidence (tickets):** TSSD-1753

**Preconditions:**
- Prepare test data to reproduce the scenario described by the ticket(s)

**Steps:**
- Log in as a user who can access the feature (admin if required)
- Navigate to the feature area relevant to this claim

**Expected:**
- System behavior matches expected handling for this edge case (no unexpected errors)

**Selector hints:**
- Prefer data-testid selectors (or stable automation IDs) where available
- Fallback: locate by visible text labels and section headings

### Cross Month Leave Deduction

**Statement:** Leave request created in one month, processed in another

**Meta:** id=JIRA_027 | playwright=YES | test_data=YES

**Evidence (tickets):** TSSD-1807

**Preconditions:**
- Prepare test data to reproduce the scenario described by the ticket(s)

**Steps:**
- Log in as a user who can access the feature (admin if required)
- Navigate to the feature area relevant to this claim
- Perform the action described by the ticket evidence

**Expected:**
- System behavior matches expected handling for this edge case (no unexpected errors)

**Selector hints:**
- Prefer data-testid selectors (or stable automation IDs) where available
- Fallback: locate by visible text labels and section headings

### Non-Standard Contract Tenure

**Statement:** Handling limited contract with unpaid leave

**Meta:** id=JIRA_034 | playwright=YES

**Evidence (tickets):** TSSD-2043

**Preconditions:**
_None_

**Steps:**
- Log in as a user who can access the feature (admin if required)
- Navigate to the feature area relevant to this claim

**Expected:**
- System behavior matches expected handling for this edge case (no unexpected errors)

**Selector hints:**
- Prefer data-testid selectors (or stable automation IDs) where available
- Fallback: locate by visible text labels and section headings

### Partial Service Period Calculation

**Statement:** Handling of incomplete final year in service duration

**Meta:** id=JIRA_038 | playwright=YES | test_data=YES

**Evidence (tickets):** TSSD-2252

**Preconditions:**
- Prepare test data to reproduce the scenario described by the ticket(s)

**Steps:**
- Log in as a user who can access the feature (admin if required)
- Navigate to the feature area relevant to this claim

**Expected:**
- System behavior matches expected handling for this edge case (no unexpected errors)

**Selector hints:**
- Prefer data-testid selectors (or stable automation IDs) where available
- Fallback: locate by visible text labels and section headings

### Leap Year Boundary Condition

**Statement:** Service period spanning a leap year with non-standard day counting

**Meta:** id=JIRA_053 | playwright=YES

**Evidence (tickets):** TSSD-4179

**Preconditions:**
_None_

**Steps:**
- Log in as a user who can access the feature (admin if required)
- Navigate to the feature area relevant to this claim

**Expected:**
- System behavior matches expected handling for this edge case (no unexpected errors)

**Selector hints:**
- Prefer data-testid selectors (or stable automation IDs) where available
- Fallback: locate by visible text labels and section headings

### Partial Month Leave Deduction

**Statement:** Unpaid leave spanning multiple months with non-standard divisor

**Meta:** id=JIRA_059 | playwright=NO | test_data=YES | blocker=BACKEND_ORACLE_REQUIRED

**Evidence (tickets):** TSSD-4368

**Preconditions:**
- Prepare test data to reproduce the scenario described by the ticket(s)

**Steps:**
- Log in as a user who can access the feature (admin if required)
- Navigate to the feature area relevant to this claim

**Expected:**
- System behavior matches expected handling for this edge case (no unexpected errors)

**Selector hints:**
- Prefer data-testid selectors (or stable automation IDs) where available
- Fallback: locate by visible text labels and section headings

### Mid Month Departure Handling

**Statement:** Employee leaving partway through month with monthly accrual policy

**Meta:** id=JIRA_066 | playwright=YES | test_data=YES

**Evidence (tickets):** TSSD-4742

**Preconditions:**
- Prepare test data to reproduce the scenario described by the ticket(s)

**Steps:**
- Log in as a user who can access the feature (admin if required)
- Navigate to the feature area relevant to this claim

**Expected:**
- System behavior matches expected handling for this edge case (no unexpected errors)

**Selector hints:**
- Prefer data-testid selectors (or stable automation IDs) where available
- Fallback: locate by visible text labels and section headings

### Working Days Salary Calculation

**Statement:** Handling employees working more or fewer than 26 days per month

**Meta:** id=JIRA_071 | playwright=YES | test_data=YES

**Evidence (tickets):** TSSD-4905

**Preconditions:**
- Prepare test data to reproduce the scenario described by the ticket(s)

**Steps:**
- Log in as a user who can access the feature (admin if required)
- Navigate to the feature area relevant to this claim

**Expected:**
- System behavior matches expected handling for this edge case (no unexpected errors)

**Selector hints:**
- Prefer data-testid selectors (or stable automation IDs) where available
- Fallback: locate by visible text labels and section headings

## Troubleshooting

### Unpaid Leave Deduction Verification

**Statement:** Unpaid Leave Deduction Verification

**Meta:** id=JIRA_016 | playwright=YES | test_data=YES

**Evidence (tickets):** TSSD-1301, TSSD-1753

**Preconditions:**
- Prepare test data to reproduce the scenario described by the ticket(s)

**Steps:**
- Log in as a user who can access the feature (admin if required)
- Navigate to the feature area relevant to this claim

**Expected:**
- The issue state is resolved OR the UI reaches a stable expected state after applying the steps

**Selector hints:**
- Prefer data-testid selectors (or stable automation IDs) where available
- Fallback: locate by visible text labels and section headings

### Unpaid Leave Deduction Workaround

**Statement:** Unpaid Leave Deduction Workaround

**Meta:** id=JIRA_019 | playwright=YES | test_data=YES

**Evidence (tickets):** TSSD-1581

**Preconditions:**
- Prepare test data to reproduce the scenario described by the ticket(s)

**Steps:**
- Log in as a user who can access the feature (admin if required)
- Navigate to the feature area relevant to this claim

**Expected:**
- The issue state is resolved OR the UI reaches a stable expected state after applying the steps

**Selector hints:**
- Prefer data-testid selectors (or stable automation IDs) where available
- Fallback: locate by visible text labels and section headings

### Manual Calculation Verification

**Statement:** Manual Calculation Verification

**Meta:** id=JIRA_023 | playwright=YES | test_data=YES

**Evidence (tickets):** TSSD-1625

**Preconditions:**
- Prepare test data to reproduce the scenario described by the ticket(s)

**Steps:**
- Log in as a user who can access the feature (admin if required)
- Navigate to the feature area relevant to this claim

**Expected:**
- The issue state is resolved OR the UI reaches a stable expected state after applying the steps

**Selector hints:**
- Prefer data-testid selectors (or stable automation IDs) where available
- Fallback: locate by visible text labels and section headings

### Daily Rate Calculation Workaround

**Statement:** Daily Rate Calculation Workaround

**Meta:** id=JIRA_029 | playwright=YES | test_data=YES

**Evidence (tickets):** TSSD-1807

**Preconditions:**
- Prepare test data to reproduce the scenario described by the ticket(s)

**Steps:**
- Log in as a user who can access the feature (admin if required)
- Navigate to the feature area relevant to this claim

**Expected:**
- The issue state is resolved OR the UI reaches a stable expected state after applying the steps

**Selector hints:**
- Prefer data-testid selectors (or stable automation IDs) where available
- Fallback: locate by visible text labels and section headings

### Gratuity Calculation Verification

**Statement:** Gratuity Calculation Verification

**Meta:** id=JIRA_033 | playwright=YES | ui=YES | test_data=YES

**Evidence (tickets):** TSSD-2043

**Preconditions:**
- Prepare test data to reproduce the scenario described by the ticket(s)

**Steps:**
- Log in as a user who can access the feature (admin if required)
- Navigate to the feature area relevant to this claim
- Locate the UI element referenced in the claim (label/field/value/tooltip/etc.)

**Expected:**
- The issue state is resolved OR the UI reaches a stable expected state after applying the steps

**Selector hints:**
- Prefer data-testid selectors (or stable automation IDs) where available
- Fallback: locate by visible text labels and section headings

### Gratuity Calculation Discrepancy Resolution

**Statement:** Gratuity Calculation Discrepancy Resolution

**Meta:** id=JIRA_036 | playwright=YES | ui=YES | test_data=YES

**Evidence (tickets):** TSSD-2252

**Preconditions:**
- Prepare test data to reproduce the scenario described by the ticket(s)

**Steps:**
- Log in as a user who can access the feature (admin if required)
- Navigate to the feature area relevant to this claim
- Locate the UI element referenced in the claim (label/field/value/tooltip/etc.)

**Expected:**
- The issue state is resolved OR the UI reaches a stable expected state after applying the steps

**Selector hints:**
- Prefer data-testid selectors (or stable automation IDs) where available
- Fallback: locate by visible text labels and section headings

### Daily Wage Calculation Troubleshooting

**Statement:** Daily Wage Calculation Troubleshooting

**Meta:** id=JIRA_039 | playwright=YES | test_data=YES

**Evidence (tickets):** TSSD-2321

**Preconditions:**
- Prepare test data to reproduce the scenario described by the ticket(s)

**Steps:**
- Log in as a user who can access the feature (admin if required)
- Navigate to the feature area relevant to this claim

**Expected:**
- The issue state is resolved OR the UI reaches a stable expected state after applying the steps

**Selector hints:**
- Prefer data-testid selectors (or stable automation IDs) where available
- Fallback: locate by visible text labels and section headings

### EOS Calculation Troubleshooting

**Statement:** EOS Calculation Troubleshooting

**Meta:** id=JIRA_044 | playwright=YES | test_data=YES

**Evidence (tickets):** TSSD-2605

**Preconditions:**
- Prepare test data to reproduce the scenario described by the ticket(s)

**Steps:**
- Log in as a user who can access the feature (admin if required)
- Navigate to the feature area relevant to this claim

**Expected:**
- The issue state is resolved OR the UI reaches a stable expected state after applying the steps

**Selector hints:**
- Prefer data-testid selectors (or stable automation IDs) where available
- Fallback: locate by visible text labels and section headings

### Daily Wage Formula Troubleshooting

**Statement:** Daily Wage Formula Troubleshooting

**Meta:** id=JIRA_045 | playwright=NO | ui=YES | blocker=BACKEND_ORACLE_REQUIRED

**Evidence (tickets):** TSSD-2648

**Preconditions:**
_None_

**Steps:**
- Log in as a user who can access the feature (admin if required)
- Navigate to the feature area relevant to this claim
- Locate the UI element referenced in the claim (label/field/value/tooltip/etc.)

**Expected:**
- The issue state is resolved OR the UI reaches a stable expected state after applying the steps

**Selector hints:**
- Prefer data-testid selectors (or stable automation IDs) where available
- Fallback: locate by visible text labels and section headings

### Leave Encashment Configuration Workaround

**Statement:** Leave Encashment Configuration Workaround

**Meta:** id=JIRA_049 | playwright=YES

**Evidence (tickets):** TSSD-3648

**Preconditions:**
_None_

**Steps:**
- Log in as a user who can access the feature (admin if required)
- Navigate to the feature area relevant to this claim
- Open the relevant Settings / Configuration section for the feature

**Expected:**
- The issue state is resolved OR the UI reaches a stable expected state after applying the steps

**Selector hints:**
- Prefer data-testid selectors (or stable automation IDs) where available
- Fallback: locate by visible text labels and section headings

### Leap Year Calculation Workaround

**Statement:** Leap Year Calculation Workaround

**Meta:** id=JIRA_054 | playwright=YES | test_data=YES

**Evidence (tickets):** TSSD-4179

**Preconditions:**
- Prepare test data to reproduce the scenario described by the ticket(s)

**Steps:**
- Log in as a user who can access the feature (admin if required)
- Navigate to the feature area relevant to this claim

**Expected:**
- The issue state is resolved OR the UI reaches a stable expected state after applying the steps

**Selector hints:**
- Prefer data-testid selectors (or stable automation IDs) where available
- Fallback: locate by visible text labels and section headings

### Daily Wage Proration Verification

**Statement:** Daily Wage Proration Verification

**Meta:** id=JIRA_055 | playwright=YES | test_data=YES

**Evidence (tickets):** TSSD-4292

**Preconditions:**
- Prepare test data to reproduce the scenario described by the ticket(s)

**Steps:**
- Log in as a user who can access the feature (admin if required)
- Navigate to the feature area relevant to this claim

**Expected:**
- The issue state is resolved OR the UI reaches a stable expected state after applying the steps

**Selector hints:**
- Prefer data-testid selectors (or stable automation IDs) where available
- Fallback: locate by visible text labels and section headings

### Leave Deduction Manual Reconciliation

**Statement:** Leave Deduction Manual Reconciliation

**Meta:** id=JIRA_058 | playwright=YES | test_data=YES

**Evidence (tickets):** TSSD-4368

**Preconditions:**
- Prepare test data to reproduce the scenario described by the ticket(s)

**Steps:**
- Log in as a user who can access the feature (admin if required)
- Navigate to the feature area relevant to this claim

**Expected:**
- The issue state is resolved OR the UI reaches a stable expected state after applying the steps

**Selector hints:**
- Prefer data-testid selectors (or stable automation IDs) where available
- Fallback: locate by visible text labels and section headings

### Leave Salary Calculation Verification

**Statement:** Leave Salary Calculation Verification

**Meta:** id=JIRA_063 | playwright=YES | test_data=YES

**Evidence (tickets):** TSSD-4731

**Preconditions:**
- Prepare test data to reproduce the scenario described by the ticket(s)

**Steps:**
- Log in as a user who can access the feature (admin if required)
- Navigate to the feature area relevant to this claim

**Expected:**
- The issue state is resolved OR the UI reaches a stable expected state after applying the steps

**Selector hints:**
- Prefer data-testid selectors (or stable automation IDs) where available
- Fallback: locate by visible text labels and section headings

### Manual EOS Adjustment

**Statement:** Manual EOS Adjustment

**Meta:** id=JIRA_065 | playwright=YES

**Evidence (tickets):** TSSD-4742

**Preconditions:**
_None_

**Steps:**
- Log in as a user who can access the feature (admin if required)
- Navigate to the feature area relevant to this claim

**Expected:**
- The issue state is resolved OR the UI reaches a stable expected state after applying the steps

**Selector hints:**
- Prefer data-testid selectors (or stable automation IDs) where available
- Fallback: locate by visible text labels and section headings

### Manual Salary Adjustment

**Statement:** Manual Salary Adjustment

**Meta:** id=JIRA_069 | playwright=YES

**Evidence (tickets):** TSSD-4905

**Preconditions:**
_None_

**Steps:**
- Log in as a user who can access the feature (admin if required)
- Navigate to the feature area relevant to this claim

**Expected:**
- The issue state is resolved OR the UI reaches a stable expected state after applying the steps

**Selector hints:**
- Prefer data-testid selectors (or stable automation IDs) where available
- Fallback: locate by visible text labels and section headings

## Customer Request

### Gratuity Calculation Clarification

**Statement:** Transparent explanation of gratuity calculation methodology

**Meta:** id=JIRA_021 | playwright=NO | ui=YES | test_data=YES | support=NOT_SUPPORTED | blocker=BACKEND_ORACLE_REQUIRED

**Evidence (tickets):** TSSD-1625

**Preconditions:**
- Prepare test data to reproduce the scenario described by the ticket(s)

**Steps:**
- Log in as a user who can access the feature (admin if required)
- Navigate to the feature area relevant to this claim
- Locate the UI element referenced in the claim (label/field/value/tooltip/etc.)

**Expected:**
- Requested capability is not present OR is not configurable in the UI (consistent with NOT_SUPPORTED)

**Selector hints:**
- Prefer data-testid selectors (or stable automation IDs) where available
- Fallback: locate by visible text labels and section headings

### Unpaid Leave Calculation Accuracy

**Statement:** Accurate unpaid leave deduction calculations based on specific leave policy configurations

**Meta:** id=JIRA_026 | playwright=YES | test_data=YES | support=NOT_SUPPORTED

**Evidence (tickets):** TSSD-1753

**Preconditions:**
- Prepare test data to reproduce the scenario described by the ticket(s)

**Steps:**
- Log in as a user who can access the feature (admin if required)
- Navigate to the feature area relevant to this claim
- Open the relevant Settings / Configuration section for the feature

**Expected:**
- Requested capability is not present OR is not configurable in the UI (consistent with NOT_SUPPORTED)

**Selector hints:**
- Prefer data-testid selectors (or stable automation IDs) where available
- Fallback: locate by visible text labels and section headings

### Daily Rate Display Clarity

**Statement:** Show daily rate for the specific month of leave occurrence

**Meta:** id=JIRA_030 | playwright=YES | ui=YES | test_data=YES | support=NOT_SUPPORTED

**Evidence (tickets):** TSSD-1807

**Preconditions:**
- Prepare test data to reproduce the scenario described by the ticket(s)

**Steps:**
- Log in as a user who can access the feature (admin if required)
- Navigate to the feature area relevant to this claim
- Locate the UI element referenced in the claim (label/field/value/tooltip/etc.)

**Expected:**
- Requested capability is not present OR is not configurable in the UI (consistent with NOT_SUPPORTED)

**Selector hints:**
- Prefer data-testid selectors (or stable automation IDs) where available
- Fallback: locate by visible text labels and section headings

### Alternative Daily Wage Formula

**Statement:** Allow daily wage calculation as (Basic × 12 / 365)

**Meta:** id=JIRA_031 | playwright=NO | ui=YES | test_data=YES | support=NOT_SUPPORTED | blocker=BACKEND_ORACLE_REQUIRED

**Evidence (tickets):** TSSD-2043

**Preconditions:**
- Prepare test data to reproduce the scenario described by the ticket(s)

**Steps:**
- Log in as a user who can access the feature (admin if required)
- Navigate to the feature area relevant to this claim
- Locate the UI element referenced in the claim (label/field/value/tooltip/etc.)

**Expected:**
- Requested capability is not present OR is not configurable in the UI (consistent with NOT_SUPPORTED)

**Selector hints:**
- Prefer data-testid selectors (or stable automation IDs) where available
- Fallback: locate by visible text labels and section headings

### Gratuity Calculation Transparency

**Statement:** Detailed explanation of gratuity calculation methodology

**Meta:** id=JIRA_037 | playwright=NO | ui=YES | test_data=YES | support=NOT_SUPPORTED | blocker=BACKEND_ORACLE_REQUIRED

**Evidence (tickets):** TSSD-2252

**Preconditions:**
- Prepare test data to reproduce the scenario described by the ticket(s)

**Steps:**
- Log in as a user who can access the feature (admin if required)
- Navigate to the feature area relevant to this claim
- Locate the UI element referenced in the claim (label/field/value/tooltip/etc.)

**Expected:**
- Requested capability is not present OR is not configurable in the UI (consistent with NOT_SUPPORTED)

**Selector hints:**
- Prefer data-testid selectors (or stable automation IDs) where available
- Fallback: locate by visible text labels and section headings

### Unified Daily Wage Calculation

**Statement:** Consistent daily wage calculation across leave salary and salary proration

**Meta:** id=JIRA_041 | playwright=YES | test_data=YES | support=NOT_SUPPORTED

**Evidence (tickets):** TSSD-2321

**Preconditions:**
- Prepare test data to reproduce the scenario described by the ticket(s)

**Steps:**
- Log in as a user who can access the feature (admin if required)
- Navigate to the feature area relevant to this claim

**Expected:**
- Requested capability is not present OR is not configurable in the UI (consistent with NOT_SUPPORTED)

**Selector hints:**
- Prefer data-testid selectors (or stable automation IDs) where available
- Fallback: locate by visible text labels and section headings

### EOS Amount Accuracy

**Statement:** Accurate pro-rated end of service amount calculation

**Meta:** id=JIRA_043 | playwright=YES | test_data=YES | support=SUPPORTED

**Evidence (tickets):** TSSD-2605

**Preconditions:**
- Prepare test data to reproduce the scenario described by the ticket(s)

**Steps:**
- Log in as a user who can access the feature (admin if required)
- Navigate to the feature area relevant to this claim

**Expected:**
- Requested capability is not present OR is not configurable in the UI (consistent with NOT_SUPPORTED)

**Selector hints:**
- Prefer data-testid selectors (or stable automation IDs) where available
- Fallback: locate by visible text labels and section headings

### Formula Consistency Request

**Statement:** Consistent daily wage formula across leave policy and payroll modules

**Meta:** id=JIRA_046 | playwright=NO | ui=YES | support=NOT_SUPPORTED | blocker=BACKEND_ORACLE_REQUIRED

**Evidence (tickets):** TSSD-2648

**Preconditions:**
_None_

**Steps:**
- Log in as a user who can access the feature (admin if required)
- Navigate to the feature area relevant to this claim
- Locate the UI element referenced in the claim (label/field/value/tooltip/etc.)

**Expected:**
- Requested capability is not present OR is not configurable in the UI (consistent with NOT_SUPPORTED)

**Selector hints:**
- Prefer data-testid selectors (or stable automation IDs) where available
- Fallback: locate by visible text labels and section headings

### Consistent Leave Accrual Calculation

**Statement:** Maintain consistent daily wage calculations across months

**Meta:** id=JIRA_048 | playwright=YES | test_data=YES | support=NOT_SUPPORTED

**Evidence (tickets):** TSSD-3648

**Preconditions:**
- Prepare test data to reproduce the scenario described by the ticket(s)

**Steps:**
- Log in as a user who can access the feature (admin if required)
- Navigate to the feature area relevant to this claim

**Expected:**
- Requested capability is not present OR is not configurable in the UI (consistent with NOT_SUPPORTED)

**Selector hints:**
- Prefer data-testid selectors (or stable automation IDs) where available
- Fallback: locate by visible text labels and section headings

### Accurate Leap Year Gratuity

**Statement:** Correct End of Service gratuity calculation for leap years

**Meta:** id=JIRA_051 | playwright=YES | ui=YES | test_data=YES | support=NOT_SUPPORTED

**Evidence (tickets):** TSSD-4179

**Preconditions:**
- Prepare test data to reproduce the scenario described by the ticket(s)

**Steps:**
- Log in as a user who can access the feature (admin if required)
- Navigate to the feature area relevant to this claim
- Locate the UI element referenced in the claim (label/field/value/tooltip/etc.)

**Expected:**
- Requested capability is not present OR is not configurable in the UI (consistent with NOT_SUPPORTED)

**Selector hints:**
- Prefer data-testid selectors (or stable automation IDs) where available
- Fallback: locate by visible text labels and section headings

### Unpaid Leave Deduction Clarity

**Statement:** Understand why unpaid leave deduction exceeds expected monthly salary

**Meta:** id=JIRA_060 | playwright=YES | test_data=YES | support=NOT_SUPPORTED

**Evidence (tickets):** TSSD-4368

**Preconditions:**
- Prepare test data to reproduce the scenario described by the ticket(s)

**Steps:**
- Log in as a user who can access the feature (admin if required)
- Navigate to the feature area relevant to this claim

**Expected:**
- Requested capability is not present OR is not configurable in the UI (consistent with NOT_SUPPORTED)

**Selector hints:**
- Prefer data-testid selectors (or stable automation IDs) where available
- Fallback: locate by visible text labels and section headings

### Flexible Daily Wage Divisor

**Statement:** Ability to dynamically adjust daily wage calculation divisor based on actual calendar days

**Meta:** id=JIRA_061 | playwright=NO | test_data=YES | support=NOT_SUPPORTED | blocker=BACKEND_ORACLE_REQUIRED

**Evidence (tickets):** TSSD-4731

**Preconditions:**
- Prepare test data to reproduce the scenario described by the ticket(s)

**Steps:**
- Log in as a user who can access the feature (admin if required)
- Navigate to the feature area relevant to this claim

**Expected:**
- Requested capability is not present OR is not configurable in the UI (consistent with NOT_SUPPORTED)

**Selector hints:**
- Prefer data-testid selectors (or stable automation IDs) where available
- Fallback: locate by visible text labels and section headings

### Leave Balance Inclusion

**Statement:** Include leave days for departure month in EOS calculation

**Meta:** id=JIRA_064 | playwright=YES | test_data=YES | support=NOT_SUPPORTED

**Evidence (tickets):** TSSD-4742

**Preconditions:**
- Prepare test data to reproduce the scenario described by the ticket(s)

**Steps:**
- Log in as a user who can access the feature (admin if required)
- Navigate to the feature area relevant to this claim

**Expected:**
- Requested capability is not present OR is not configurable in the UI (consistent with NOT_SUPPORTED)

**Selector hints:**
- Prefer data-testid selectors (or stable automation IDs) where available
- Fallback: locate by visible text labels and section headings

### Automated Salary Proration

**Statement:** Implement automated salary calculation based on 26 working days per month

**Meta:** id=JIRA_068 | playwright=YES | test_data=YES | support=NOT_SUPPORTED

**Evidence (tickets):** TSSD-4905

**Preconditions:**
- Prepare test data to reproduce the scenario described by the ticket(s)

**Steps:**
- Log in as a user who can access the feature (admin if required)
- Navigate to the feature area relevant to this claim

**Expected:**
- Requested capability is not present OR is not configurable in the UI (consistent with NOT_SUPPORTED)

**Selector hints:**
- Prefer data-testid selectors (or stable automation IDs) where available
- Fallback: locate by visible text labels and section headings

## Known Issue

### Global Daily Rate Configuration Overrides Policy Settings

**Statement:** System applies a global daily rate configuration that supersedes individual leave policy calculations, potentially causing incorrect salary deductions

**Meta:** id=JIRA_001 | playwright=YES | ui=YES | test_data=YES | limitation_type=configuration_problem

**Evidence (tickets):** TSSD-1753

**Preconditions:**
- Prepare test data to reproduce the scenario described by the ticket(s)

**Steps:**
- Log in as a user who can access the feature (admin if required)
- Navigate to the feature area relevant to this claim
- Open the relevant Settings / Configuration section for the feature
- Locate the UI element referenced in the claim (label/field/value/tooltip/etc.)

**Expected:**
- UI state matches the claim expectation and does not show contradictory information

**Selector hints:**
- Prefer data-testid selectors (or stable automation IDs) where available
- Fallback: locate by visible text labels and section headings

### Incorrect Daily Rate Display Across Payroll Months

**Statement:** The system recalculates daily rates based on the payroll processing month instead of storing the rate from the original leave occurrence month, causing user confusion.

**Meta:** id=JIRA_002 | playwright=NO | ui=YES | test_data=YES | limitation_type=ux_issue | blocker=ASYNC_RECALC_OR_TXN_LIFECYCLE

**Evidence (tickets):** TSSD-1807

**Preconditions:**
- Prepare test data to reproduce the scenario described by the ticket(s)

**Steps:**
- Log in as a user who can access the feature (admin if required)
- Navigate to the feature area relevant to this claim
- Locate the UI element referenced in the claim (label/field/value/tooltip/etc.)

**Expected:**
- UI state matches the claim expectation and does not show contradictory information

**Selector hints:**
- Prefer data-testid selectors (or stable automation IDs) where available
- Fallback: locate by visible text labels and section headings

### Gratuity Calculation Methodology Nuances

**Statement:** The platform's gratuity calculation involves complex logic for handling partial years and leap year adjustments, which can differ from manual calculations

**Meta:** id=JIRA_003 | playwright=NO | ui=YES | test_data=YES | limitation_type=calculation_complexity | blocker=BACKEND_ORACLE_REQUIRED

**Evidence (tickets):** TSSD-2252

**Preconditions:**
- Prepare test data to reproduce the scenario described by the ticket(s)

**Steps:**
- Log in as a user who can access the feature (admin if required)
- Navigate to the feature area relevant to this claim
- Locate the UI element referenced in the claim (label/field/value/tooltip/etc.)

**Expected:**
- UI state matches the claim expectation and does not show contradictory information

**Selector hints:**
- Prefer data-testid selectors (or stable automation IDs) where available
- Fallback: locate by visible text labels and section headings

### Leave Salary Calculation Mismatch

**Statement:** The system calculates leave salary and salary proration using independent daily wage configurations with different divisors (30 vs 30.42 days), causing calculation mismatches.

**Meta:** id=JIRA_004 | playwright=NO | test_data=YES | limitation_type=configuration_problem | blocker=BACKEND_ORACLE_REQUIRED

**Evidence (tickets):** TSSD-2321

**Preconditions:**
- Prepare test data to reproduce the scenario described by the ticket(s)

**Steps:**
- Log in as a user who can access the feature (admin if required)
- Navigate to the feature area relevant to this claim
- Open the relevant Settings / Configuration section for the feature

**Expected:**
- Result is observable (in UI or logs) and consistent with the claim statement

**Selector hints:**
- Prefer data-testid selectors (or stable automation IDs) where available
- Fallback: locate by visible text labels and section headings

### Incorrect Pro-Rated End of Service Amount Calculation

**Statement:** Daily wage calculation for pro-rated amounts consistently produces amounts less than expected by 0.1 or 0.2 decimal points, affecting all employees' end of service settlements.

**Meta:** id=JIRA_005 | playwright=NO | test_data=YES | limitation_type=calculation_bug | blocker=BACKEND_ORACLE_REQUIRED

**Evidence (tickets):** TSSD-2605

**Preconditions:**
- Prepare test data to reproduce the scenario described by the ticket(s)

**Steps:**
- Log in as a user who can access the feature (admin if required)
- Navigate to the feature area relevant to this claim

**Expected:**
- Result is observable (in UI or logs) and consistent with the claim statement

**Selector hints:**
- Prefer data-testid selectors (or stable automation IDs) where available
- Fallback: locate by visible text labels and section headings

### Daily Wage Formula Inconsistency Between Leave Policy and Payroll

**Statement:** Payroll settings override leave policy daily wage formula for unpaid leave, causing user confusion about calculation method

**Meta:** id=JIRA_006 | playwright=NO | ui=YES | test_data=YES | limitation_type=ux_issue | blocker=BACKEND_ORACLE_REQUIRED

**Evidence (tickets):** TSSD-2648

**Preconditions:**
- Prepare test data to reproduce the scenario described by the ticket(s)

**Steps:**
- Log in as a user who can access the feature (admin if required)
- Navigate to the feature area relevant to this claim
- Open the relevant Settings / Configuration section for the feature
- Locate the UI element referenced in the claim (label/field/value/tooltip/etc.)

**Expected:**
- UI state matches the claim expectation and does not show contradictory information

**Selector hints:**
- Prefer data-testid selectors (or stable automation IDs) where available
- Fallback: locate by visible text labels and section headings

### Leave Salary Accrual Calculation Inconsistency

**Statement:** Daily wage calculator configured to use working days causes monthly calculation variations in leave salary accruals

**Meta:** id=JIRA_007 | playwright=YES | test_data=YES | limitation_type=configuration_problem

**Evidence (tickets):** TSSD-3648

**Preconditions:**
- Prepare test data to reproduce the scenario described by the ticket(s)

**Steps:**
- Log in as a user who can access the feature (admin if required)
- Navigate to the feature area relevant to this claim
- Open the relevant Settings / Configuration section for the feature

**Expected:**
- Result is observable (in UI or logs) and consistent with the claim statement

**Selector hints:**
- Prefer data-testid selectors (or stable automation IDs) where available
- Fallback: locate by visible text labels and section headings

### End of Service Gratuity Calculation Incorrect for Leap Years

**Statement:** The system incorrectly calculates service days by using 365 days instead of 366 days for leap years, specifically 2024, resulting in an inaccurate gratuity amount.

**Meta:** id=JIRA_008 | playwright=YES | ui=YES | test_data=YES | limitation_type=calculation_bug

**Evidence (tickets):** TSSD-4179

**Preconditions:**
- Prepare test data to reproduce the scenario described by the ticket(s)

**Steps:**
- Log in as a user who can access the feature (admin if required)
- Navigate to the feature area relevant to this claim
- Locate the UI element referenced in the claim (label/field/value/tooltip/etc.)

**Expected:**
- UI state matches the claim expectation and does not show contradictory information

**Selector hints:**
- Prefer data-testid selectors (or stable automation IDs) where available
- Fallback: locate by visible text labels and section headings

### Leave Salary Calculation Discrepancy

**Statement:** Daily wage calculation follows a fixed policy setting that might not align with user expectations about calendar days and prorated calculations

**Meta:** id=JIRA_009 | playwright=YES | test_data=YES | limitation_type=calculation_configuration

**Evidence (tickets):** TSSD-4731

**Preconditions:**
- Prepare test data to reproduce the scenario described by the ticket(s)

**Steps:**
- Log in as a user who can access the feature (admin if required)
- Navigate to the feature area relevant to this claim

**Expected:**
- Result is observable (in UI or logs) and consistent with the claim statement

**Selector hints:**
- Prefer data-testid selectors (or stable automation IDs) where available
- Fallback: locate by visible text labels and section headings

### Incorrect Days Display in Unpaid Leave Remarks

**Statement:** The platform displays incorrect remarks for daily wage calculations in February, showing 31 days instead of the actual 29 days for leap years, despite accurate backend calculation

**Meta:** id=JIRA_010 | playwright=YES | ui=YES | test_data=YES | limitation_type=ux_issue

**Evidence (tickets):** TSSD-1301

**Preconditions:**
- Prepare test data to reproduce the scenario described by the ticket(s)

**Steps:**
- Log in as a user who can access the feature (admin if required)
- Navigate to the feature area relevant to this claim
- Locate the UI element referenced in the claim (label/field/value/tooltip/etc.)

**Expected:**
- UI state matches the claim expectation and does not show contradictory information

**Selector hints:**
- Prefer data-testid selectors (or stable automation IDs) where available
- Fallback: locate by visible text labels and section headings

### Gratuity Calculation Leap Year Discrepancy

**Statement:** Platform potentially miscalculates service duration by not consistently handling February 29th in leap years, causing minor gratuity amount variations

**Meta:** id=JIRA_011 | playwright=YES | ui=YES | test_data=YES | limitation_type=calculation_precision

**Evidence (tickets):** TSSD-1625

**Preconditions:**
- Prepare test data to reproduce the scenario described by the ticket(s)

**Steps:**
- Log in as a user who can access the feature (admin if required)
- Navigate to the feature area relevant to this claim
- Locate the UI element referenced in the claim (label/field/value/tooltip/etc.)

**Expected:**
- UI state matches the claim expectation and does not show contradictory information

**Selector hints:**
- Prefer data-testid selectors (or stable automation IDs) where available
- Fallback: locate by visible text labels and section headings

### Extra Day Added in Gratuity Calculation

**Statement:** System calculates 623 days instead of 622 days when computing employee job tenure

**Meta:** id=JIRA_012 | playwright=NO | ui=YES | test_data=YES | limitation_type=calculation_bug | blocker=ASYNC_RECALC_OR_TXN_LIFECYCLE

**Evidence (tickets):** TSSD-2043

**Preconditions:**
- Prepare test data to reproduce the scenario described by the ticket(s)

**Steps:**
- Log in as a user who can access the feature (admin if required)
- Navigate to the feature area relevant to this claim
- Locate the UI element referenced in the claim (label/field/value/tooltip/etc.)
- Perform the action described by the ticket evidence

**Expected:**
- UI state matches the claim expectation and does not show contradictory information

**Selector hints:**
- Prefer data-testid selectors (or stable automation IDs) where available
- Fallback: locate by visible text labels and section headings

### Salary Proration Calculation Confusion

**Statement:** User misunderstood how working days are calculated based on work week configuration. The system was calculating correctly, but the user did not initially understand that their specific work week (NVK Operations) had all 7 days configured as working days.

**Meta:** id=JIRA_013 | playwright=YES | test_data=YES | limitation_type=configuration_problem

**Evidence (tickets):** TSSD-4292

**Preconditions:**
- Prepare test data to reproduce the scenario described by the ticket(s)

**Steps:**
- Log in as a user who can access the feature (admin if required)
- Navigate to the feature area relevant to this claim
- Open the relevant Settings / Configuration section for the feature

**Expected:**
- Result is observable (in UI or logs) and consistent with the claim statement

**Selector hints:**
- Prefer data-testid selectors (or stable automation IDs) where available
- Fallback: locate by visible text labels and section headings

### System vs Manual Gratuity Calculation Difference

**Statement:** Slight variation between system-calculated gratuity (AED 231,821.10) and manual calculation (AED 252,485)

**Meta:** id=JIRA_014 | playwright=YES | ui=YES | test_data=YES | limitation_type=calculation_precision

**Evidence (tickets):** TSSD-4742

**Preconditions:**
- Prepare test data to reproduce the scenario described by the ticket(s)

**Steps:**
- Log in as a user who can access the feature (admin if required)
- Navigate to the feature area relevant to this claim
- Locate the UI element referenced in the claim (label/field/value/tooltip/etc.)

**Expected:**
- UI state matches the claim expectation and does not show contradictory information

**Selector hints:**
- Prefer data-testid selectors (or stable automation IDs) where available
- Fallback: locate by visible text labels and section headings

### Unpaid Leave Deduction Calculation

**Statement:** System failed to recalculate pending unpaid leave deductions when new salary configurations were created after leave request submission, resulting in incorrect payroll deductions

**Meta:** id=JIRA_018 | playwright=NO | test_data=YES | limitation_type=calculation_bug | calculation_complexity | blocker=ASYNC_RECALC_OR_TXN_LIFECYCLE

**Evidence (tickets):** TSSD-1581, TSSD-4368

**Preconditions:**
- Prepare test data to reproduce the scenario described by the ticket(s)

**Steps:**
- Log in as a user who can access the feature (admin if required)
- Navigate to the feature area relevant to this claim
- Open the relevant Settings / Configuration section for the feature
- Perform the action described by the ticket evidence

**Expected:**
- Result is observable (in UI or logs) and consistent with the claim statement

**Selector hints:**
- Prefer data-testid selectors (or stable automation IDs) where available
- Fallback: locate by visible text labels and section headings

